<!DOCTYPE html>
<html>
<head>
  <title>Task Details</title>

  <link href="/output.css" rel="stylesheet" type="text/css">
  <style>
    /* Add custom CSS styles */
    .description-container {
      max-height: 200px; /* Adjust the max height as needed */
      overflow-y: auto; /* Enable vertical scrolling for overflowed content */
      white-space: normal; /* Allow text to wrap within the container */
      word-wrap: break-word; /* Wrap long words or URLs */
    }
  </style>
</head>
<body class="bg-gray-900 text-white flex justify-center items-center h-screen">
  <div class="w-1/2 bg-gray-800 shadow-lg rounded-lg p-4">
    <% if (task) { %>
    <h1 class="text-3xl font-semibold mb-4"><%= task.title %></h1>
    <div class="description-container">
      <p class="text-gray-200"><%= task.description %></p>
    </div>
    <p class="text-gray-300">
      Created At: <%= task.createdAt ? task.createdAt.toLocaleString() : 'N/A' %>
    </p>
    <p class="text-gray-300">
      Updated At: <%= task.updatedAt ? task.updatedAt.toLocaleString() : 'N/A' %>
    </p>

    <div class="mt-4">
      <a href="/tasks/edit/<%= task.id %>"
         class="px-4 py-2 bg-blue-500 hover:bg-blue-600 text-white rounded-full inline-block">
        Edit Task
      </a>

      <form method="POST" action="/tasks/<%= task.id %>?_method=DELETE"
            onsubmit="return confirm('Are you sure you want to delete this task?')"
            class="inline-block">
        <button type="submit"
                class="px-4 py-2 bg-red-500 hover:bg-red-600 text-white rounded-full">
          Delete Task
        </button>
      </form>
    </div>

    <% } else { %>
    <p class="text-red-600 mt-4">Task not found.</p>
    <% } %>

    <a href="/"
       class="block mt-4 text-blue-500 hover:underline">
      Back to Task List
    </a>
  </div>
</body>
</html>
