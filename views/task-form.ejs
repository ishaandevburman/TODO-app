<!DOCTYPE html>
<html>

<head>
  <title>
    <%= mode === 'add' ? 'Add Task' : 'Edit Task' %>
  </title>
  <!-- Include the DaisyUI CSS file -->
  <link href="/output.css" rel="stylesheet" type="text/css">
</head>

<body class="bg-gray-900 text-white flex justify-center items-center h-screen">
  <div class="w-1/2 bg-gray-800 shadow-lg rounded-lg p-4">
    <h1 class="text-3xl font-semibold mb-4">
      <%= mode === 'add' ? 'Add Task' : 'Edit Task' %>
    </h1>
    <form method="POST" action="<%= mode === 'add' ? '/tasks' : '/tasks/' + task.id + '?_method=PUT' %>"
      class="space-y-4">
      <div class="flex flex-col space-y-2">
        <input type="text" id="title" name="title" value="<%= task.title %>" required
          placeholder="Title (e.g., Task Title)"
          class="w-full px-3 py-2 rounded-lg border-gray-300 focus:border-blue-500 focus:ring focus:ring-blue-200">
      </div>
      <div class="flex flex-col space-y-2">
        <textarea id="description" name="description" required rows="6"
          placeholder="Description (e.g., Task Description)"
          class="w-full px-3 py-2 rounded-lg border-gray-300 focus:border-blue-500 focus:ring focus:ring-blue-200"><%= task.description %></textarea>
      </div>
      <button type="submit"
        class="w-full py-2 px-4 bg-blue-500 hover:bg-blue-600 text-white rounded-full text-lg">
        <%= mode === 'add' ? 'Add' : 'Update' %> Task
      </button>
    </form>
    <a href="/"
      class="block mt-4 text-blue-500 hover:underline">
      Back to Task List
    </a>
  </div>
</body>

</html>
